#include<bits/stdc++.h>
using namespace std;

int main(){
    vector < pair < int, int > > vp;
    vp.push_back({1,2});
    vp.push_back({3,5});
    
    for(auto u : vp) cout << u.first << " " << u.second << endl;

}